// 本地开发覆盖文件示例：复制为 config.local.js（已在 .gitignore，不入库）后生效。
// 仓库默认 ACTIVE='prod'（USE_MOCK=false）；仅本地开发切 'dev' 走 mock 数据。
// 提审/发布前必须确认 prod 生效（删除/改名 config.local.js 即可），且页面「模拟模式」角标不出现。
module.exports = { ACTIVE: 'dev' };
